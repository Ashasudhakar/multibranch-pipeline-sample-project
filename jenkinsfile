// Declarative //
pipeline {
    agent {label 'ubuntu'}
    stages {
        stage('Example') {
            steps {
                sh '''
                date
                echo 'Hello World'
                '''
            }
        }
    }
}