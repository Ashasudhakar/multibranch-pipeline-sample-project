// Declarative //
pipeline {
    agent {label 'ubuntu'}
    options {timestamps()}
    stages {
        stage('Example') {
            steps {
                sh '''
                date
                echo 'Hello World'
                '''
            }
        }
    }
}