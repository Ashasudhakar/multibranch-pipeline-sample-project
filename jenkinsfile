// Declarative //
pipeline {
    agent {label 'ubuntu'}
    options {timestamp()}
    stages {
        stage('Example') {
            steps {
                sh '''
                date
                echo 'Hello World'
                '''
            }
        }
    }
}